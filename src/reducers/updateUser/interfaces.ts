export interface UpdateUserPasswordTypes {
  currentPassword: string;
  newPassword: string;
}

export interface UpdateUserFullNameTypes {
  currentPassword: string;
  firstName: string;
  lastName: string;
}

export interface UpdateUserEmailTypes {
  currentPassword: string;
  newEmail: string;
}
